// This is the msg include for the example topic to be send from the roscore 
// on 192.168.1.1 to the roscore on 192.168.1.2, assuming you have not modified 
// the base_ip in the config YAML file. This line must be in both remote ros_pound nodes
#include <std_msgs/String.h>

